[build]
builder = "nixpacks"
buildCommand = "pip install playwright playwright-stealth && playwright install chromium --with-deps"

[deploy]
startCommand = "python backend/serve.py"
healthcheckPath = "/api/ping"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
